# Пустой baseURL — запросы идут на тот же хост, Vite проксирует /api → localhost:5080
# VITE_API_URL=

# Если backend на другом хосте (без прокси Vite):
# VITE_API_URL=http://localhost:5080

# Куда проксировать в dev/preview (vite.config.js):
# VITE_API_PROXY=http://localhost:5080
