<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFrameworks>net9.0-android;net9.0-ios;net9.0-maccatalyst</TargetFrameworks>
		<TargetFrameworks Condition="$([MSBuild]::IsOSPlatform('windows'))">$(TargetFrameworks);net9.0-windows10.0.19041.0</TargetFrameworks>
		<!-- Uncomment to also build the tizen app. You will need to install tizen by following this: https://github.com/Samsung/Tizen.NET -->
		<!-- <TargetFrameworks>$(TargetFrameworks);net9.0-tizen</TargetFrameworks> -->

		<!-- Note for MacCatalyst:
		The default runtime is maccatalyst-x64, except in Release config, in which case the default is maccatalyst-x64;maccatalyst-arm64.
		When specifying both architectures, use the plural <RuntimeIdentifiers> instead of the singular <RuntimeIdentifier>.
		The Mac App Store will NOT accept apps with ONLY maccatalyst-arm64 indicated;
		either BOTH runtimes must be indicated or ONLY macatalyst-x64. -->
		<!-- For example: <RuntimeIdentifiers>maccatalyst-x64;maccatalyst-arm64</RuntimeIdentifiers> -->

		<OutputType>Exe</OutputType>
		<RootNamespace>ProAqua.App</RootNamespace>
		<UseMaui>true</UseMaui>
		<SingleProject>true</SingleProject>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>

		<!-- Display name -->
		<ApplicationTitle>ПроАква</ApplicationTitle>

		<!-- App Identifier -->
		<ApplicationId>com.proaqua.app</ApplicationId>

		<!-- Versions — Android login ANR / native HTTP -->
		<ApplicationDisplayVersion>1.6.4</ApplicationDisplayVersion>
		<ApplicationVersion>20</ApplicationVersion>

		<!-- To develop, package, and publish an app to the Microsoft Store, see: https://aka.ms/MauiTemplateUnpackaged -->
		<WindowsPackageType>None</WindowsPackageType>

		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'ios'">15.0</SupportedOSPlatformVersion>
		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'maccatalyst'">15.0</SupportedOSPlatformVersion>
		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'">21.0</SupportedOSPlatformVersion>
		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'windows'">10.0.17763.0</SupportedOSPlatformVersion>
		<TargetPlatformMinVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'windows'">10.0.17763.0</TargetPlatformMinVersion>
		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'tizen'">6.5</SupportedOSPlatformVersion>
	</PropertyGroup>

	<!--
	  Android F5 / VS InDebugMode: reads AndroidIncludeDebugSymbols (else DebugSymbols) at
	  static evaluation time — NOT from build Targets. Xamarin.Android.Common.targets Choose
	  overwrites AndroidIncludeDebugSymbols after normal csproj PropertyGroups, so we also
	  force again in Directory.Build.targets + ProAqua.ForceAndroidDebugSymbols.targets
	  (AfterMicrosoftNETSdkTargets, after the Choose). Forced for ALL Android configs so
	  Release toolbar selection still allows deploy/debug attach.
	-->
	<PropertyGroup Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'">
		<AndroidIncludeDebugSymbols>true</AndroidIncludeDebugSymbols>
		<DebugSymbols>true</DebugSymbols>
		<AndroidManagedSymbols>true</AndroidManagedSymbols>
		<DebugType>portable</DebugType>
		<DebuggerSupport>true</DebuggerSupport>
	</PropertyGroup>
	<PropertyGroup Condition="$([System.String]::Copy('$(Configuration)').StartsWith('Debug'))">
		<Optimize>false</Optimize>
	</PropertyGroup>
	<PropertyGroup Condition="'$(Configuration)|$(TargetFramework)|$(Platform)'=='Release|net9.0-android|AnyCPU'">
	  <AndroidPackageFormat>apk</AndroidPackageFormat>
	  <!-- Sideload: trimming/AOT sometimes crash on login when replacing Window.Page -->
	  <PublishTrimmed>false</PublishTrimmed>
	  <RunAOTCompilation>false</RunAOTCompilation>
	</PropertyGroup>

	<ItemGroup>
		<!-- App Icon: логотип ПроАква -->
		<MauiIcon Include="Resources\AppIcon\appicon.png" Color="#000000" BaseSize="512,512" />

		<!-- Splash Screen — тот же логотип -->
		<MauiSplashScreen Include="Resources\Splash\splash.png" Color="#000000" BaseSize="512,512" Resize="True" />

		<!-- Images -->
		<!-- Photos are shipped at their native size: Resize/BaseSize upscaled them into
		     multi-megabyte density drawables that failed to decode on device. -->
		<MauiImage Include="Resources\Images\*" />
		<MauiImage Update="Resources\Images\dotnet_bot.png" Resize="True" BaseSize="300,185" />
		<MauiImage Update="Resources\Images\logo.png" Resize="True" BaseSize="256,256" />

		<!-- Custom Fonts -->
		<MauiFont Include="Resources\Fonts\*" />

		<!-- Raw Assets (also remove the "Resources\Raw" prefix) -->
		<MauiAsset Include="Resources\Raw\**" LogicalName="%(RecursiveDir)%(Filename)%(Extension)" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.Maui.Controls" Version="$(MauiVersion)" />
		<PackageReference Include="Microsoft.Extensions.Logging.Debug" Version="9.0.9" />
	</ItemGroup>

</Project>
