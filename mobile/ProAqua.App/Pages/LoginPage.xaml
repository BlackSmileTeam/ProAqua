<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProAqua.App.Pages.LoginPage"
             BackgroundColor="{StaticResource MidnightBlue}"
             Title="ПроАква"
             Shell.NavBarIsVisible="False">
    <VerticalStackLayout Padding="28" Spacing="18" VerticalOptions="Center">
        <Image Source="app-icon.png" WidthRequest="64" HeightRequest="64" HorizontalOptions="Center" />
        <Label Text="ПроАква" FontSize="36" FontAttributes="Bold" TextColor="{StaticResource Primary}" HorizontalTextAlignment="Center" />
        <Label Text="Мойка и детейлинг"
               TextColor="{StaticResource Gray300}"
               FontSize="14"
               HorizontalTextAlignment="Center"
               Margin="0,-8,0,8" />

        <Border StrokeThickness="0" StrokeShape="RoundRectangle 22" BackgroundColor="{StaticResource Gray900}" Padding="16,2">
            <Entry x:Name="PhoneEntry"
                   Placeholder="Телефон"
                   Keyboard="Telephone"
                   TextColor="White"
                   PlaceholderColor="{StaticResource Gray400}"
                   BackgroundColor="Transparent"
                   HeightRequest="48" />
        </Border>

        <Border StrokeThickness="0" StrokeShape="RoundRectangle 22" BackgroundColor="{StaticResource Gray900}" Padding="16,2">
            <Entry x:Name="PasswordEntry"
                   Placeholder="Пароль"
                   IsPassword="True"
                   TextColor="White"
                   PlaceholderColor="{StaticResource Gray400}"
                   BackgroundColor="Transparent"
                   HeightRequest="48" />
        </Border>

        <Button Text="Войти"
                Clicked="OnLoginClicked"
                BackgroundColor="{StaticResource Primary}"
                TextColor="White"
                FontAttributes="Bold"
                CornerRadius="22"
                HeightRequest="52" />
        <Label x:Name="ErrorLabel" TextColor="#FB7185" HorizontalTextAlignment="Center" />
    </VerticalStackLayout>
</ContentPage>
